//package.json manages the details.
// npm init to install file fill details and see magic

// npm i packages install any one will create node modules folder and all 
//packages will be there

//PACKAGE.JSON IMPORTANT H BHAI 

const { color, log } = require('console-log-colors');
const { red, green, cyan } = color;

console.log(cyan("color.green('text')"))
log.red("log.red('text')", 'test');   /// just usage 

const colors = require('colors');
console.log("yahaa".bgWhite)

//nodejs is single threaded // one code at a time .