// what happens if node modules is deleted 
// npm install - does again the previous packages only 

//push in git
// dont push node modules in git never 
//create .gitignore file and inside it write /node_modules to not push node
//modules .. whenever user runs npm install all is done