#include <iostream>
using namespace std;


// relational :- ==, !=, >, <, >=, <= 
// logical :- &&, ||, !
// bitwise :- &, |, ^, ~, <<, >>
// a<<n = a*2^n
// a>>n = a/2^n
// Assignment :- =, +=, -=, *=, /=
// miscellaneous :- sizeof(a), condition ? x : y (if this : else), 
//char ch= 'a' int(a)(cast), a=(2,4) -4 assigned
//& - address of the variable, * - pointer  

int main() {
    int a = 10;
    cout<<a++<<endl; //++a--> 11

    int b=1;
    cout<<b++ + ++b;// 4 pre and post //



}